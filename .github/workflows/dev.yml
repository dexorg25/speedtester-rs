name: dev

on:
  push:
    branches: [ "dev" ]

jobs:
  build:
    name: develop
    runs-on: self-hosted
    
    steps:
      - run: gh pr create -B main -f
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
